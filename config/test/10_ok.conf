server {
    listen 1234
    server_name www.ilRECh.rs
    client_body_size 1000000

    # no URL has bee specified, so location won't be parsed correctly
    location {
        root config/web_pages
    }
}
